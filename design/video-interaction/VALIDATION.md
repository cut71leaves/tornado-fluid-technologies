# 视频与互动流体验证记录

验证日期：2026-09-17。

- 71 个 HTML 文件的品牌名称和本地链接、媒体资源检查通过。
- 全站 118 次页面与尺寸组合检查、27 项原有交互检查通过；无缺失图片、失效链接、脚本错误或整页横向溢出。
- 首屏原有动效 13 项检查通过，包含播放、暂停、手机资源选择、循环、减少动态效果、网络失败与本地文件访问。
- 新增 22 项视频与互动检查通过，覆盖三个视频解码、完整画幅、音量/进度、原生全屏、互斥播放、离屏与后台停播、弹窗关闭和焦点恢复、用户暂停保持、鼠标形变、可逆滚动、静止时停止渲染、视频播放时暂停装饰、360/390 像素手机模式、减少动态效果、WebGL/脚本加载失败、视频失败重试以及本地文件访问。
- 宣传材料中的三个原视频 SHA-256 均保持不变；0、1 号转码前后的 AAC 音频包一致；2 号网页副本与原文件逐字节相同。
- 已检查三个首页段落的桌面、手机视觉效果，全部视频保持 16:9 完整画幅，AI 生成标识和原有视频文字保留。
- Three.js 本地包约 492 KB，gzip 后约 125 KB；在手机、触屏和减少动态效果模式下不请求该包，桌面靠近展示区才加载。

浏览器验证使用本机 Chromium；手机检查为模拟视口，后台行为通过 visibilitychange 状态切换验证。未将这些结果表述为全机型或所有浏览器真机认证。

首屏和雕塑已采用统一动态偏好通知，避免系统偏好切换时两处显示状态不同步。所有验证脚本与重现说明保留在仓库。
